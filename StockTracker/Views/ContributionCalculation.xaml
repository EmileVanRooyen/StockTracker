<UserControl x:Class="StockTracker.Views.ContributionCalculation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StockTracker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Contribution Calculation" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Add Row" Click="AddRow_Click" Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="Remove Selected" Click="RemoveRow_Click" Padding="10,5"/>
        </StackPanel>

        <DataGrid Grid.Row="2" 
                  x:Name="ContributionDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  SelectionChanged="ContributionDataGrid_SelectionChanged"
                  CellEditEnding="ContributionDataGrid_CellEditEnding">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fixed Costs" Binding="{Binding FixedCosts, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Variable Costs" Binding="{Binding VariableCosts, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Contribution Margin Ratio" Binding="{Binding ContributionMarginRatio, UpdateSourceTrigger=PropertyChanged, StringFormat=N3}" Width="*"/>
                <DataGridTextColumn Header="Selling Price" Binding="{Binding SellingPrice, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Contribution per unit" Binding="{Binding ContributionPerUnit, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Breakeven Quantity (Units)" Binding="{Binding BreakevenQuantity, UpdateSourceTrigger=PropertyChanged, StringFormat=N0}" Width="*"/>
                <DataGridTextColumn Header="Breakeven Value (Rand)" Binding="{Binding BreakevenValue, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Separator Grid.Row="3" Margin="0,20,0,10"/>

        <Grid Grid.Row="4" x:Name="CalculationDetailsPanel" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Calculation Details for Selected Row" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>

            <DataGrid Grid.Row="1" 
                      x:Name="CalculationDetailsGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="All"
                      RowBackground="White"
                      AlternatingRowBackground="#F5F5F5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Field" Binding="{Binding Field}" Width="2*" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="Formula" Binding="{Binding Formula}" Width="3*"/>
                    <DataGridTextColumn Header="Substituted Values" Binding="{Binding SubstitutedValues}" Width="3*"/>
                    <DataGridTextColumn Header="Result" Binding="{Binding Result}" Width="2*" FontWeight="Bold"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>