<Window x:Class="StockTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StockTracker"
        xmlns:views="clr-namespace:StockTracker.Views"
        mc:Ignorable="d"
        Title="FIFO Stock Tracker" Height="700" Width="1300"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <TabControl x:Name="mainTabControl" 
                    Margin="0" 
                    Background="White"
                    BorderThickness="0">
            <!-- Tab Control Style -->
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="20,10"/>
                    <Setter Property="Background" Value="#ECF0F1"/>
                    <Setter Property="Foreground" Value="#2C3E50"/>
                    <Setter Property="BorderBrush" Value="#BDC3C7"/>
                    <Setter Property="BorderThickness" Value="1,1,1,0"/>
                    <Setter Property="Margin" Value="2,0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" 
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="5,5,0,0"
                                        Margin="{TemplateBinding Margin}">
                                    <ContentPresenter x:Name="ContentSite"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Center"
                                                    ContentSource="Header"
                                                    Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="White"/>
                                        <Setter Property="Foreground" Value="#2C3E50"/>
                                        <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0"/>
                                        <Setter Property="Panel.ZIndex" Value="100"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Border" Property="Background" Value="#ECF0F1"/>
                                        <Setter Property="Foreground" Value="#7F8C8D"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#D5DBDB"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- Stock Tracker Tab -->
            <TabItem Header="Inventory Valuation">
                <views:StockTrackerView/>
            </TabItem>

            <!-- Contribution Calculation Tab -->
            <TabItem Header="Mixed Cost Analysis">
                <views:ContributionCalculation/>
            </TabItem>

            <!-- Lead Time Contribution Analysis Tab -->
            <TabItem Header="Lead Time, Usage, Safety Stock, Reorder Point">
                <views:LeadTimeContributionAnalysis/>
            </TabItem>
            <TabItem Header="VAT Calculation">
                <views:VATCalculation/>
            </TabItem>
            <TabItem Header="Sales and Direct Labour Budget">
                <views:LabourCalculation/>
            </TabItem>
            <TabItem Header="EOQ Calculation">
                <views:EOQCalculator/>
            </TabItem>
            <TabItem Header="Mark-up Calculation">
                <views:MarkUpCalculation/>
            </TabItem>
            <TabItem Header="Fixed Cost Portion Calculation">
                <views:FixedCostPortionCalculator/>
            </TabItem>

            <!-- Add more tabs here as needed -->
        </TabControl>
    </Grid>
</Window>